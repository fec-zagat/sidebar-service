import React from 'react';

const FaceTime = () => (
  <div>
    <span>
      Open FaceTime?
    </span>
    <span>
      Always open these types of links in the associated app
    </span>
    <button type="button">Cancel</button>
    <button type="button">Open FaceTime</button>
  </div>
);


export default FaceTime;
